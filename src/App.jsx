import { Canvas, useFrame, useThree } from '@react-three/fiber';
import { PointerLockControls, Sky } from '@react-three/drei';
import { Suspense, useCallback, useEffect, useRef, useState } from 'react';
import * as THREE from 'three';
import World from './components/World';
import Dog from './components/Dog';
import GeneratedModel from './components/GeneratedModel';

function Player({ onInteract }) {
  const { camera } = useThree(); const keys = useRef({});
  useEffect(() => { const down=e=>{keys.current[e.code]=true;if(e.code==='KeyE')onInteract()}; const up=e=>{keys.current[e.code]=false}; addEventListener('keydown',down);addEventListener('keyup',up);return()=>{removeEventListener('keydown',down);removeEventListener('keyup',up)} },[onInteract]);
  useFrame((_,delta)=>{window.__worldCamera=camera;const speed=4.8,forward=new THREE.Vector3();camera.getWorldDirection(forward);forward.y=0;forward.normalize();const right=new THREE.Vector3().crossVectors(forward,camera.up).normalize();if(keys.current.KeyW)camera.position.addScaledVector(forward,speed*delta);if(keys.current.KeyS)camera.position.addScaledVector(forward,-speed*delta);if(keys.current.KeyA)camera.position.addScaledVector(right,speed*delta);if(keys.current.KeyD)camera.position.addScaledVector(right,-speed*delta);camera.position.y=1.8;camera.position.x=THREE.MathUtils.clamp(camera.position.x,-45,45);camera.position.z=THREE.MathUtils.clamp(camera.position.z,-45,45)});
  return null;
}
function bark(){try{const A=window.AudioContext||window.webkitAudioContext,c=new A(),o=c.createOscillator(),g=c.createGain();o.type='sawtooth';o.frequency.setValueAtTime(170,c.currentTime);o.frequency.exponentialRampToValueAtTime(85,c.currentTime+.16);g.gain.setValueAtTime(.001,c.currentTime);g.gain.exponentialRampToValueAtTime(.22,c.currentTime+.015);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+.2);o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+.21)}catch{}}
async function generate3D(file,onStage){onStage('Reading your photo');const imageBase64=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)});onStage('Sending image to Hunyuan3D');const response=await fetch('/api/generate-3d',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({imageBase64})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'3D generation request failed');if(!data.result)throw new Error('3D service returned no model.');onStage('Loading your 3D companion');return data.result;}
export default function App(){const animalRef=useRef(),fileRef=useRef();const[notice,setNotice]=useState(''),[started,setStarted]=useState(false),[photo,setPhoto]=useState(null),[modelUrl,setModelUrl]=useState(null),[worldName,setWorldName]=useState('Forest Companion'),[generating,setGenerating]=useState(false),[stage,setStage]=useState('');const notify=useCallback(text=>{setNotice(text);setTimeout(()=>setNotice(''),3500)},[]);const interact=useCallback(()=>{if(!animalRef.current||!window.__worldCamera)return;const d=window.__worldCamera.position.distanceTo(animalRef.current.position);if(d<5.5){animalRef.current.userData.react?.();bark();notify('🐕 WOOF! Your companion noticed you.')}else notify('Walk closer to your companion first.')},[notify]);const upload=async e=>{const f=e.target.files?.[0];if(!f)return;if(!f.type.startsWith('image/'))return notify('Please choose an image.');setPhoto(URL.createObjectURL(f));setWorldName(`${f.name.replace(/\.[^.]+$/,'')} • AI World`);setModelUrl(null);setGenerating(true);setStage('Starting…');try{const glb=await generate3D(f,setStage);setModelUrl(glb);setStage('3D companion ready');notify('✨ Your photo became a 3D companion!')}catch(err){console.error(err);setStage('Generation unavailable');notify(`⚠️ ${err?.message||'3D generation failed'}`)}finally{setGenerating(false)}};const enter=()=>{setStarted(true);document.querySelector('canvas')?.requestPointerLock?.()};return <main className="app"><Canvas shadows camera={{position:[0,1.8,8],fov:70}}><color attach="background" args={['#9bc8e8']}/><fog attach="fog" args={['#9bc8e8',18,65]}/><ambientLight intensity={1.1}/><directionalLight castShadow position={[8,14,6]} intensity={2.2} shadow-mapSize={[2048,2048]}/><Sky sunPosition={[100,30,20]} turbidity={8} rayleigh={1.5}/><Suspense fallback={null}><World/>{modelUrl?<GeneratedModel url={modelUrl} modelRef={animalRef}/>:<Dog dogRef={animalRef}/>}<Player onInteract={interact}/></Suspense><PointerLockControls onLock={()=>setStarted(true)}/></Canvas>{!started&&<section className="start-card"><div className="badge">CODE2CREATE • PHOTO → WORLD</div><h1>Turn a photo into an<br/><span>interactive world.</span></h1><p>Upload an animal photo and our 3D pipeline turns it into a companion you can explore with.</p><button className="secondary" disabled={generating} onClick={()=>fileRef.current?.click()}>📷 {generating?'Generating 3D…':photo?'Change Animal Photo':'Upload Animal Photo'}</button><input ref={fileRef} type="file" accept="image/*" onChange={upload} hidden/>{photo&&<div className="photo-preview"><img src={photo} alt="Uploaded animal"/><div><b>{worldName}</b><small>{generating?`⚙ ${stage}`:modelUrl?'✓ Real 3D model generated':`⚠ ${stage}`}</small></div></div>}{generating&&<div className="generation"><div className="spinner"/><b>{stage}</b><small>Remote AI GPU is creating the 3D model. This may take a few minutes.</small></div>}<button className="enter" disabled={generating} onClick={enter}>Enter World →</button><small>WASD move • Mouse look • E interact</small></section>}<div className="hud"><div className="brand">WORLD<span>LAB</span></div><div className="controls">WASD MOVE &nbsp; • &nbsp; E INTERACT &nbsp; • &nbsp; ESC EXIT</div></div><div className="crosshair">+</div>{notice&&<div className="notice">{notice}</div>}{started&&<div className="photo-pill">{modelUrl?'✨ AI 3D WORLD':photo?'📷 PHOTO WORLD':'🌲 DEMO WORLD'} <span>•</span> {worldName}</div>}</main>}
